import { loadConnection, getSession } from './config.js';
async function parseResponse(response){const text=await response.text();try{return JSON.parse(text)}catch{throw new Error('Resposta inválida do backend. Verifique a implantação do Apps Script.')}}
function credentials(){const c=loadConnection();if(!c.endpoint||!c.key)throw new Error('Configure a URL do Apps Script e a APP_KEY.');return c}
export async function apiGet(action,params={}){const {endpoint,key}=credentials();const url=new URL(endpoint);url.searchParams.set('action',action);url.searchParams.set('key',key);const token=getSession();if(token)url.searchParams.set('token',token);Object.entries(params).forEach(([k,v])=>v!==undefined&&v!==null&&url.searchParams.set(k,String(v)));const data=await parseResponse(await fetch(url));if(!data.ok)throw new Error(data.error||'Falha na integração.');return data}
export async function apiPost(action,payload={}){const {endpoint,key}=credentials();const response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action,key,token:getSession(),...payload})});const data=await parseResponse(response);if(!data.ok)throw new Error(data.error||'Falha na integração.');return data}
export async function login(username,password){const {endpoint,key}=credentials();const response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({action:'login',key,username,password})});const data=await parseResponse(response);if(!data.ok)throw new Error(data.error||'Login inválido.');return data}
